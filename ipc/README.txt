You need to run follwoing steps

1. Start Minix VM with submitted minix image.
2. login with follwoing credentials to minix
	Username: root
	Password: root123

3. type follwoing command to compile the user program.
 	gcc welcome.c
4. Run ./a.out from multiple terminals. You will see a menu and select any of the options from
   the menu to make different system calls.


